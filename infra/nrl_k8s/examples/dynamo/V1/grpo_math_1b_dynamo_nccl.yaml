# GRPO math 1B example with Dynamo direct generation + NCCL refit.
#
# This is a small functional bring-up variant of
# examples/configs/grpo_math_1B.yaml. It preserves the source base model, but
# uses NeMo-RL's passthrough prompt template plus bounded smoke-test sampling.
# Two steps verify that the Dynamo DGD receives a native vLLM NCCL refit.

defaults: ../../../../../examples/configs/grpo_math_1B.yaml

grpo:
  num_prompts_per_step: 2
  num_generations_per_prompt: 2
  max_rollout_turns: 1
  max_num_steps: 2
  val_period: 0
  val_at_start: false
  val_at_end: false
  max_val_samples: 4
  val_batch_size: 4

checkpointing:
  enabled: false

policy:
  model_name: "Qwen/Qwen2.5-1.5B"
  tokenizer:
    name: ${policy.model_name}
    chat_template: null
    chat_template_kwargs: null
  train_global_batch_size: ${mul:${grpo.num_prompts_per_step}, ${grpo.num_generations_per_prompt}}
  train_micro_batch_size: 1
  logprob_batch_size: 1
  max_total_sequence_length: 512

  dtensor_cfg:
    enabled: true
    _v2: false
    cpu_offload: false
    activation_checkpointing: false
    sequence_parallel: false
    tensor_parallel_size: 1
    context_parallel_size: 1
    custom_parallel_plan: null

  megatron_cfg:
    enabled: false

  generation:
    backend: "dynamo"
    max_new_tokens: 128
    temperature: 0.7
    top_p: 0.95
    top_k: 50
    stop_token_ids: null
    stop_strings: null
    dynamo_cfg:
      engine_world_size: 1
      frontend_url: null
      namespace: null
      frontend_port: 8000
      dyn_system_port: 9090
      request_timeout_s: 900.0
      discovery_timeout_s: 15.0
      control_timeout_s: 600.0
      exclude_tools_when_tool_choice_none: true
      metrics_include_prefixes: null
      metrics_exclude_prefixes: null
    vllm_cfg:
      async_engine: false
      tensor_parallel_size: 1
      pipeline_parallel_size: 1
      expert_parallel_size: 1
      gpu_memory_utilization: 0.6
      load_format: dummy
      expose_http_server: false
      env_vars: null
      max_model_len: ${policy.max_total_sequence_length}
    colocated:
      enabled: false

logger:
  log_dir: "logs/grpo-math-1b-dynamo-nccl"
  num_val_samples_to_print: 0
  wandb_enabled: false
  tensorboard_enabled: false
  mlflow_enabled: false
  swanlab_enabled: false
  monitor_gpus: false

cluster:
  gpus_per_node: 1
  num_nodes: 1
